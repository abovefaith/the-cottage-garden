import { CountryDataStore } from './country-data-store.model';

describe('CountryDataStore', () => {
  it('should create an instance', () => {
    expect(new CountryDataStore()).toBeTruthy();
  });
});
